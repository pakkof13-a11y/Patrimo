/**
 * Segment catch-all : l'UI vit dans layout.tsx (PortfolioApp).
 * Cette page ne sert qu'à activer les routes /dashboard, /positions/pea, …
 * sans re-monter le shell à chaque navigation.
 */
export default function PortfolioCatchAllPage() {
  return null;
}
